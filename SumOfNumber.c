#include <stdio.h>
int main()
{
    int n;
    printf("Enter the number: ");
    scanf("%d",&n);
    int sum=0;
    while(n!=0) {
        n/10
        sum++;
    }
    printf("the sum of digits is %d",sum);
   return 0;
}